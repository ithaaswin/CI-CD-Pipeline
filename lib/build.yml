---
- hosts: all
  gather_facts: no
  tags: iTrust
  tasks:
  - name: ping all hosts
    ping:
  - name: apt-get update
    become: yes
    apt:
      update_cache: yes
  - name: install nodejs
    apt: nodejs
  